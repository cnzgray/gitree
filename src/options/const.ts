import { GithubProfile, GiteaProfile } from '@/store/types'

export const DEFAULT_GITHUB_PROFILE: GithubProfile = Object.freeze({ urls: [], accessKey: '' })
export const DEFAULT_GITEA_PROFILE: GiteaProfile = Object.freeze({ url: '', accessKey: '' })
